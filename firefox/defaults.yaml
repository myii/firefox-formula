# -*- coding: utf-8 -*-
# vim: ft=yaml
---
firefox:
  pkg:
    name: firefox_x64
  rootgroup: root
  config: '/etc/firefox'
  service:
    name: firefox
  subcomponent:
    config: '/etc/firefox-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
