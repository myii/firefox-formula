# -*- coding: utf-8 -*-
# vim: ft=yaml
---
template__:
  pkg:
    name: template__
  rootgroup: root
  config: '/etc/template__'
  service:
    name: template__
  subcomponent:
    config: '/etc/template__-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
